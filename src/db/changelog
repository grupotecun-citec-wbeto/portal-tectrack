# Changelog for Database Versioning

- version: 1
    name_sqlite: TECTRACK
    mysql_schema_version: 1.0.0  
    date: YYYY-MM-DD
    description: Initial version of the database schema.
    changes:
        - Added tables:
                - users
                - orders
                - products
        - Created relationships between tables.
        - Added initial indexes and constraints.

- version: 1.1.0
    date: YYYY-MM-DD
    description: Added new features and optimizations.
    changes:
        - Added column `email` to `users` table.
        - Optimized indexes on `orders` table.
        - Updated constraints for `products` table.

- version: 1.2.0
    date: YYYY-MM-DD
    description: Bug fixes and minor updates.
    changes:
        - Fixed null constraint issue in `users` table.
        - Updated default values for `products` table.
        - Removed deprecated column `old_price` from `products` table.